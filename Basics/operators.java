public class operators {
    public static void main(String[] args) {
        int a = 10 ; 
        int b = 5 ; 
        int op = a+b ;  // it does ( + , -  , * , / , %) Same as the C++ we did before
        System.out.println(op);
        System.out.println(Math.max(a, b));
        System.out.println((int)(Math.random()*100)); //Checks random value upto 100    
    }
}
